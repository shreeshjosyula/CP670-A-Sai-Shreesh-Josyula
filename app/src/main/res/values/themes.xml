<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->

    <style name="Base.Theme.AndroidAssignments" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <item name="colorPrimary">@color/appBackground</item>
<!--        <item name="buttonTint">@color/buttonYellow</item>-->
        <item name="buttonTint">@color/appBackground</item>
    </style>

    <style name="Theme.AndroidAssignments" parent="Base.Theme.AndroidAssignments">
        <item name="colorPrimary">@color/appBackground</item>
        <item name="colorSecondary">@color/appBackground</item>
        <item name="buttonTint">@color/appBackground</item>

    </style>
</resources>